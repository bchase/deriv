pub fn slice_replace(bool: Bool) {
  let foo = "bar"

  // some other logic and comments to differ the src & AST

  { //$ gen target
    case bool {
      True -> todo
      False -> todo
    }
  }
}
